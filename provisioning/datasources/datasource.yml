apiVersion: 1

datasources:
  - name: TDengine
    type: tdengine
    access: proxy
    url: http://tdengine:6041     # URL pointant vers TDengine (le port expose l'API RESTful)
    user: root                   # Utilisateur TDengine
    password: taosdata           # Mot de passe TDengine
    database: sensors            # La base de données dans TDengine
    isDefault: true              # Marquer comme source de données par défaut